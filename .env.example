# Same backend the Flutter app talks to. Production (Vercel) sets this to
# the real Render URL already — this file is only a template for local dev.
VITE_API_BASE_URL=https://vanya-backend-64ja.onrender.com/v1
